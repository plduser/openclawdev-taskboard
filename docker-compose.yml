services:
  taskboard:
    build: .
    container_name: moltdev-taskboard
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data:/app/data
      - ./static:/app/static
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
